# Selenium-Internet-Speed-Bot
This app will use selenium to run the speedtest.net application at set intervals. The download, upload, and ping values will be scraped and stored as a JSON for later analysis.
